The Bison file needs to pass through bison first.
Then flex.l file through flex.
Then compile.
Then run.
Input file should be in the folder with input.c name.


Regex.l file is FIRST PHASE.
Slight Modifications are made to first phase as well.

